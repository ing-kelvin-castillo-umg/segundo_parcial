<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Create refresh_tokens table -->
    <changeSet id="005-create-refresh-tokens" author="umg">
        <createTable tableName="refresh_tokens">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" primaryKeyName="pk_refresh_tokens_id"/>
            </column>
            <column name="token" type="TEXT">
                <constraints nullable="false" unique="true" uniqueConstraintName="uk_refresh_tokens_token"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_refresh_tokens_user_id" references="users(id)"/>
            </column>
            <column name="expiry_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="revoked" type="BOOLEAN">
                <constraints nullable="false"/>
            </column>
            <column name="revoked_at" type="TIMESTAMP"/>
        </createTable>

        <!-- Create indexes for better query performance -->
        <createIndex indexName="idx_refresh_tokens_user_id" tableName="refresh_tokens">
            <column name="user_id"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>
